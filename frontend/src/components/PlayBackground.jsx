import { PlayButton } from "./PlayButton";

export const PlayBackground = (props) => {
    return (
        <div class= "bg-stone-900 pb-full pt-0 w-fill h-screen">
            <PlayButton/>
        </div>
    );
    }